package com.graphicalcsvprocessing.graphicalcsvprocessing.processors;

/**
 * an interface all processors implement to aid future extendability of the system
 *
 * at the processor stage it is assumed values such as column names (both current and new) have been validated
 */
public interface Processor {
}
